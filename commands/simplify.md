# /simplify

Simplify code or design while preserving behavior.

## Arguments

- `request`: file path, module, feature, or refactor scope.

## Workflow

Use the `simplify` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

