# /reflect

Review workflow quality, extract lessons, or evolve craft-skills guidance.

## Arguments

- `request`: reflection scope, completed task, workflow issue, or improvement goal.

## Workflow

Use the `reflect` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

