# /debug

Investigate a bug or failing behavior before applying a fix.

## Arguments

- `request`: bug report, failing command, file path, stack trace, or reproduction.

## Workflow

Use the `debug` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

