# /browser-test

Run browser-oriented verification for a completed UI or flow.

## Arguments

- `request`: route, flow, feature, or verification scope.

## Workflow

Use the `browser-test` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

