# /finalize

Finalize completed work with verification, cleanup, and reporting.

## Arguments

- `request`: optional plan/task context or verification scope.

## Workflow

Use the `finalize` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

