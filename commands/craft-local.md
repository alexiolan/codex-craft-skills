# /craft-local

Run the craft pipeline with local LLM assistance enabled.

## Arguments

- `request`: feature/task description, prompt text, or prompt-file number.

## Workflow

Use the `craft-local` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

