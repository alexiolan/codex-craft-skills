# /craft-ace

Run the ACE craft pipeline with the local LLM as default hands for eligible tasks.

## Arguments

- `request`: feature/task description, prompt text, or prompt-file number.

## Workflow

Use the `craft-ace` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

