# /implement

Implement an approved plan or narrowly scoped task.

## Arguments

- `request`: plan path, task description, or implementation instructions.

## Workflow

Use the `implement` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

