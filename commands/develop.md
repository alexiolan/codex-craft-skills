# /develop

Run bounded development from an approved plan.

## Arguments

- `request`: plan path, task list, or implementation scope.

## Workflow

Use the `develop` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

