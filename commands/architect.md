# /architect

Create or refine an implementation plan/spec before coding.

## Arguments

- `request`: feature/task description, existing spec path, or planning prompt.

## Workflow

Use the `architect` skill from this plugin.

Pass the user-provided arguments through as `$ARGUMENTS`.

