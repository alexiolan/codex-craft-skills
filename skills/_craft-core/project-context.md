# Codex Project Context Contract

Canonical source:

```text
.codex/PROJECT.md
```

Generated compatibility output:

```text
AGENTS.md
```

Codex currently reads root `AGENTS.md`, so `AGENTS.md` is generated from `.codex/PROJECT.md`.

Rules:
- Update `.codex/PROJECT.md` first.
- Regenerate `AGENTS.md` after material context changes.
- Do not hand-edit generated `AGENTS.md` unless repairing renderer output.
